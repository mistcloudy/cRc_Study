const array = [];
let text = array.length === 0
    ? 'Array is empty'
    : 'Array is not empty';

console.log(text);

const condition1 = false;
const condition2 = false;

const value = condition1
    ? 'wow!'
    : condition2
        ? 'blabla'
        : 'foo';

console.log(value);