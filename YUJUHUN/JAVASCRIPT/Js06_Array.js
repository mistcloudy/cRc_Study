const array = [1, 2, 3, 4, 5]; // Declare array

const objects = [{ name: 'puppy' }, { name: 'kity' }]; // Declare object array

console.log(objects);
console.log(objects[0]);
console.log(objects[1]);

objects.push({
    name: 'bunny'
});

console.log(objects);

// To add a new item to array

console.log(objects.length);

// Find out size of array